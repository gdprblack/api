{
  "name": "@gdprblack/api",
  "version": "1.0.0",
  "description": "",
  "main": "dist/index.js",
  "directories": {
    "doc": "docs",
    "test": "test"
  },
  "scripts": {
    "test": "npm run test:only",
    "start": "node dist/index.js",
    "predev": "npm run build:only",
    "dev": "NODE_ENV=development node dist/src/index.js",
    "build:only": "tsc -p tsconfig.json --pretty",
    "prebuild": "npm run lint",
    "build": "npm run build:only",
    "lint:only": "tslint -c tslint.json src/**/*.ts test/**/*.ts",
    "lint": "npm run lint:only",
    "postversion": "git push && git push --tags",
    "doc:only": "npm run typedoc",
    "predoc": "npm run lint",
    "doc": "npm run doc:only",
    "typedoc": "typedoc --out docs/typedoc src/",
    "clean": "rm -rf dist/"
  },
  "author": "Backend squad <backend@goin.app>",
  "license": "ISC",
  "devDependencies": {
    "@types/mongoose": "^5.2.19",
    "@types/nock": "^9.3.0",
    "@types/sinon": "^5.0.5",
    "chai": "^4.2.0",
    "chai-http": "^4.2.0",
    "dotenv": "^6.1.0",
    "json": "^9.0.6",
    "mocha": "^5.2.0",
    "nock": "^10.0.1",
    "nyc": "^13.1.0",
    "sinon": "^7.0.0",
    "ts-node": "^7.0.1",
    "tslint": "^5.11.0",
    "typedoc": "^0.13.0",
    "typescript": "^3.1.3"
  },
  "dependencies": {
    "@gdprblack/blockchain": "^1.0.1",
    "@gdprblack/secrets": "^1.0.12",
    "@types/cors": "^2.8.4",
    "@types/morgan": "^1.7.35",
    "body-parser": "^1.18.3",
    "cors": "^2.8.4",
    "express": "^4.16.4",
    "mongoose": "^5.3.4",
    "morgan": "^1.9.1",
    "rand-token": "^0.4.0"
  },
  "private": true
}
